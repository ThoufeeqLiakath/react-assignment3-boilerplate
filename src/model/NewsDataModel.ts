export default class NewsDataModel
{
    fullDescription:string;
    url:string;
    constructor(fullDescription:string, url:string)
    {
        this.fullDescription=fullDescription;
        this.url=url;
    }
}